<?php foreach( $this->users as $neighborhood ) : ?>
<div>
    <span><?=$neighborhood->getId()?></span>
    <span><?=$neighborhood->getName()?></span>
    <span><a href="/whathood/neighborhood/page?user_id=<?=$neighborhood->getId()?>">Show Neighborhoods</a></span>
</div>
<?php endforeach; ?>