<?php
$neighborhoodPolygon = $this->paginator->getItem( 0, $this->paginator->getCurrentPageNumber() );

?>

<?php echo $this->paginationControl($this->paginator,
                                    'Sliding',
                                    'application/neighborhood-polygon/neighborhood_pagination_control.phtml', 
                                    array(
                                        'paginator' => $this->paginator,
                                        'route' => 'neighborhood',
                                        
                                        'regionName' => $this->regionName,
                                        'neighborhoodName' => $this->neighborhoodName ) ); ?>

<?php
foreach( $this->paginator as $item ) :
    $form = new \Application\Form\NeighborhoodPolygonForm();
    $form->bind( $item );
echo $this->partial( 'application/neighborhood-polygon/view.phtml', 
                    array( 'form'=> $form, 'center' => $this->center ) );
endforeach;
?>

<?php echo $this->paginationControl($this->paginator,
                                    'Sliding',
                                    'application/neighborhood-polygon/neighborhood_pagination_control.phtml', 
                                    array(
                                        'paginator' => $this->paginator,
                                        'route' => 'neighborhood',
                                        'regionName' => $this->regionName,
                                        'neighborhoodName' => $this->neighborhoodName ) ); ?>